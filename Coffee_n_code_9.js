var now = new Date(milliseconds);
  console.log(now);

  // Write a function that accepts a birthdate
  // and outputs that person's One Gigasecond birthday!

  // Use jQuery to output the answer to the "result" div
function gigsecond(birthday){
  // input string as date
  var birthdateInMilliseconds = Date.parse(birthdate);
// times milliseconds by 1000 to get a gigasecond, which is 1 trillion milliseconds
  return new Date(birthdateInMilliseconds);
}

  var gigasecondBirthday(gigasecondBirthday);
